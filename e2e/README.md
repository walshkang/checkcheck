# E2E Tests (Playwright)

This folder is reserved for Playwright end-to-end tests of the static MVP (served via `python3 -m http.server`).

Commands:
- `npm run e2e:install` (installs browsers)
- `npm run e2e`
- `npm run e2e:ui`

